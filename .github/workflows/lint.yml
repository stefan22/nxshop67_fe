name: Prettier linting

on:
  push:
    branches: [main]

jobs:
  prettier:
    runs-on: ubuntu-latest

    steps:
      - name: Starts linting
        run: |
          echo "Starts linting"

      - name: Checkout
        uses: actions/checkout@v2

        with:
          node-version: '14'

      - name: npm install
        run: npm install

      - name: prettier
        run: npm run prettier:fix
       
